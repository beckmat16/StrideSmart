// Graph.js
import React from 'react';

const Graphs = () => {
    return (
        <div>
            <h2>Training Graph</h2>
            <p>This is a placeholder for your training graph.</p>
            {/* You can use a library like Chart.js or Recharts here for actual graphs */}
            <div style={{ width: '100%', height: '300px', backgroundColor: '#e0e0e0' }}>
                <p style={{ textAlign: 'center', marginTop: '130px' }}>Graph will be rendered here</p>
            </div>
        </div>
    );
};

export default Graphs;
